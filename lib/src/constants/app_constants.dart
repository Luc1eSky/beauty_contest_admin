/// defines the layout of the sign in screen
// define heights of widgets
const double titleHeight = 100;
const double signInWidgetHeight = 700;
const double designWidgetMinHeight = 200;
const double stackedWidgetHeight = titleHeight + signInWidgetHeight + designWidgetMinHeight;

// defines how wide the right side widget in
// the widescreen design is
const double maxWidthRightSide = 500;
const double maxWidthSignInWidget = 400;

/// max parameters throughout app
// defines layout of dialogs
const double dialogMaxWidth = 500;
const double cardsMaxWidth = 700;
